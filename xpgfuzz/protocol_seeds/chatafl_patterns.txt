^(?:PASS (.*)\r\n)
(.+)


^(?:USER (.*)\r\n)
(.+)


^(?:RETR (.*)\r\n)
(.+)


^(?:RCPT TO: (.*)\r\n)
(.+)


^(?:MAIL FROM: (.*)\r\n)
(.+)


^(?:EXPN (.*)\r\n)
(.+)


^(?:TYPE (.*)\r\n)
(.+)


+++++++++++++++++++

PORT 127,0,0,1,14,178
EPRT ,2,0:0:0:0:0:0:0:0,1025,

+++++++++++++++++++
观察可以发现，粒度太粗
可以提供更细粒度的语法模板+参数约束的变异的

首先，我先python离线版本实现细粒度语法模板的变异策略
然后，再去考虑xpgfuzz的集成增强


将 chatafl 从大模型提取出的那些泛化的、基于正则表达式的模板 (COMMAND (.*))，转化为一种结构化的、带参数约束的细粒度模板

