# =========================
# XPGFuzz (anonymous release)
# =========================
#
# Copy this file to `.env` and fill in values:
#   cp .env.example .env
#
# Required for any LLM/embedding features:
XPGFUZZ_LLM_API_KEY=

# OpenAI-compatible base URL. Typically ends with `/v1`.
# Examples:
#   https://www.dmxapi.com/v1
#   https://api.openai.com/v1
#   https://<your-provider>/v1
XPGFUZZ_LLM_BASE_URL=https://www.dmxapi.com/v1

# Default chat model used by seed enrichment and C-side LLM calls.
# Paper default: DeepSeek-V3.1
XPGFUZZ_LLM_MODEL=DeepSeek-V3.1

# Embeddings (RAG / vector DB). Defaults match the paper.
XPGFUZZ_EMBEDDING_MODEL=text-embedding-3-small
XPGFUZZ_EMBEDDING_BASE_URL=

# Optional (used by Python KB builder / seed enricher CLIs)
XPGFUZZ_CHROMA_DIR=./xpgfuzz/protocol_seeds/chroma_db_protocols
XPGFUZZ_CHROMA_COLLECTION=protocols_wiki

# Backward-compatible fallbacks (if you already use these names):
# OPENAI_API_KEY=
# OPENAI_BASE_URL=

